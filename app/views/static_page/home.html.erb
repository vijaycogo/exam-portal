<!DOCTYPE html>
<html>
<head>
  <title>Sign in</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    body {
      background-image: url('https://images.pexels.com/photos/1431822/pexels-photo-1431822.jpeg?cs=srgb&dl=pexels-brett-sayles-1431822.jpg&fm=jpg');
      background-size: cover;
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-position: center center;
      background-color: #FF6B6B; /* Fun background color */
      font-family: 'Comic Sans MS', cursive, sans-serif; /* Childish font */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    h2 {
      color: #FAFA33; /* Funky green header text color */
      text-align: center;
      margin-bottom: 20px;
      font-size: 36px;
    }

    .form-container {
      background-color: #b8c5bc;
      border-radius: 15px;
      box-shadow: 0px 8px 10px rgba(0, 0, 0, 0.3);
      padding: 20px;
      width: 350px;
    }

    .form-group {
      margin-bottom: 20px;
    }

    label {
      font-weight: bold;
      color: #d6226c; /* Funky green label text color */
      font-size: 18px;
    }

    .form-control {
      width: 100%;
      padding: 10px;
      border: 2px solid #008000; /* Funky green border */
      border-radius: 10px;
      font-size: 16px;
    }

    .btn-primary {
      background-color: #163d80; /* Funky pink button color */
      color: #fff;
      width: 100%;
      padding: 12px;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-size: 18px;
      transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #FF1493; /* Darker pink on hover */
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Register For Test!!</h2>
    <%= form_with(url: enroll_user_path, local: true) do |f| %>
      <div class="form-group">
        <%= f.label :first_name %>
        <%= f.text_field :first_name, class: 'form-control', required: true %>
      </div>
      <div class="form-group">
        <%= f.label :last_name %>
        <%= f.text_field :last_name, class: 'form-control', required: true %>
      </div>
      <div class="form-group">
        <%= f.label :phone_number %>
        <%= f.text_field :phone_number, class: 'form-control', required: true %>
      </div>
      <div class="form-group">
        <%= f.label :college_id %>
        <%= f.text_field :college_id, class: 'form-control', required: true %>
      </div>
      <div class="form-group">
        <%= f.label :exam_id %>
        <%= f.text_field :exam_id, class: 'form-control', required: true %>
      </div>
      <div class="form-group">
        <%= f.label :start_time %>
        <%= f.text_field :start_time, class: 'form-control', required: true %>
      </div>
      <div class="form-group">
        <%= f.submit 'Enroll User', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</body>
</html>
